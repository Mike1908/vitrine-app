import React from 'react';

/**
 * en construction
 * @returns sous titre dynamic 
 */
const DynamicText = () => {
    return (
        <div>
            <span className="dynamic-text">
                <span className="simply">Mike Useni Sefu</span>
                <span className="" id="text-target"></span>
            </span>
        </div>
    );
};

export default DynamicText;